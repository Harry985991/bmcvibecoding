{
  "name": "vibecoding",
  "version": "1.0.0",
  "main": "scripts/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node scripts/proxy-server.js",
    "dev": "node scripts/proxy-server.js",
    "check:return-archive": "node scripts/check-investment-return-archive.js",
    "check:return-archive:strict": "node scripts/check-investment-return-archive.js --strict",
    "electron": "electron .",
    "electron-dev": "electron . --dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
